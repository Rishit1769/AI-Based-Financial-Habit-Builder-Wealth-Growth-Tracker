export default function App() {
  return (
    <main className="min-h-screen p-10" style={{ background: 'var(--canvas)', color: 'var(--ink)' }}>
      <div className="card-stadium mx-auto max-w-3xl p-10 text-center">
        <p className="eyebrow">Editorial Wealth</p>
        <h1 className="wealth-display mt-4 text-6xl font-extrabold">Wealth Growth Tracker</h1>
        <p className="mt-4 text-lg" style={{ color: 'var(--muted-ink)' }}>
          Foundation initialized. Building institutional shell next.
        </p>
      </div>
    </main>
  );
}
